<div class="container-fluid">
  <h1 class="text-center"><i class="fas fa-carrot"></i> Veggies (and Fruits Too)! <i class="fas fa-lemon"></i></h1>
  <div class="row">
    <div class="col-md-5">
      <h2>Ready to Eat:</h2>

      {{#each veggies}}
        {{#unless eaten}}
          {{> veggies/veggie-block eaten=true}}
        {{/unless}}
      {{/each}}
    </div>

    <div class="col-md-7">
      <h2>Eaten:</h2>

      {{#each veggies}}
        {{#if eaten}}
          {{> veggies/veggie-block eaten=false}}
        {{/if}}
      {{/each}}
    </div>
  </div>

  <div class="row justify-content-center">
    <div class="col-12">
      <div class="card bg-transparent border-success text-center mt-3">
      <div class="card-header">
      <h2 class="text-center"><i class="fas fa-pepper-hot"></i> Add a veggie or fruit to consume</h2>
        <form class="create-form">

          <div class="form-group">
            <label for="veg" class="font-weight-bold">Veggie or Fruit:</label>
            <input type="text" id="veg" name="name">
          </div>

          <button type="submit" class="btn-danger btn-lg">Add Veggie or Fruit</button>
          </div>
        </form>
      </div>
    </div>
  </div>

</div>